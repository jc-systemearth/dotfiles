#!/usr/bin/env bash
# DEPRECATED: This work Mac uses a single git identity configured by
# bootstrap.sh / `configure_git`. Per-repo overrides (e.g. for a second
# account) are not currently used. Remove this file if you want.
exit 0
